# Cs TV by Panda Score API

### Language
* Kotlin

### Design Patterns
 * MVVM
 * Clean Architecture

### Libs
* Gson
* Retrofit
* Okhttp
* Glide
* Coroutines
* LiveData
* Flow
* ViewModel
* Navigation
* Hilt

### Requirements to run the app

You must add an `api_token` to your own `local.properties` file in order to run and fetch the results from the API

e.g.

`api_token=your_api_token_here`

You need require your api token at:

`https://app.pandascore.co/`

